# Default environment variables.
# You may need to restart any containers if making changes here.

# Set to 1 to enable theme development mode (Twig debug, null cache bins, etc).
# See web/sites/default/settings.theme_dev.php for details.
DRAINPIPE_THEME_DEV_MODE=0
