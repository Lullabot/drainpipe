version: '3'

dotenv: ['.env']

includes:
  drupal: ./vendor/lullabot/drainpipe/tasks/drupal.yml
  setup: ./vendor/lullabot/drainpipe/tasks/setup.yml
  snapshot: ./vendor/lullabot/drainpipe/tasks/snapshot.yml

tasks:
  default:
    desc: "Example command"
    cmds:
      - echo "Hello, world!"
