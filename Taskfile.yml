version: '3'

dotenv: ['.env']

includes:
  drupal: ./tasks/drupal.yml
  setup: ./tasks/setup.yml
  snapshot: ./tasks/snapshot.yml
  test: ./tasks/test.yml

tasks:
  default:
    desc: "Example command"
    cmds:
      - echo "Hello, world!"
